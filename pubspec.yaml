name: clipboard_app
description: A clipboard manager app built with Flutter

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  super_clipboard: ^0.1.0
  path_provider: ^2.0.2
  hive: ^2.0.4
  hive_flutter: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^1.0.1
  build_runner: ^2.0.6

flutter:
  uses-material-design: true
